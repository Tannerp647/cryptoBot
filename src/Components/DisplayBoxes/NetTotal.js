import "./TotalBoxes.css"

const NetTotal = () => {

    return (
        <div className="net_total_box total_boxes">
            Net Gain/Loss Display
        </div>
    );
};

export default NetTotal;